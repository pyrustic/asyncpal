# project name
project_name = 'Asyncpal'

# project's website or README
project_url = '/README.md'

# package directory (relative path)
pkg_dir = 'asyncpal'

# API directory (relative path)
api_dir = 'docs/api'
